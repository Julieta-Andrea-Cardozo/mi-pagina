# Notas de terminal - TP1

comandos que use para hacer el trabajo practico

"pwd": Muestra la ruta del directorio actual. | Para verificar en qué carpeta estaba antes de crear el proyecto. 
`
"ls ": Lista los archivos y carpetas del directorio actual. | Para comprobar que se creó la carpeta (mi-pagina-web). 
"cs mi-pirimera-pagina": Cambia al directorio (mi-primera-pagina) | Para entrar a la carpeta del proyecto.
" mkdir images": Crea una nueva carpeta llamada (imagenes):  Para crear la carpeta donde guardar las imágenes. 
"touch style.css" : Crea un archivo vacío llamado style.css  Para generar el archivo de estilos. 

"which git": Muestra la ruta donde está instalado Git. | Para conocer la ubicación del ejecutable de Git (lo pide el punto 5). 
`code  : Abre el directorio actual en VS Code. | Para iniciar el editor desde la terminal y trabajar en el proyecto. 
git --version : Muestra la versión de Git instalada. | Para verificar que Git se instaló correctamente.
git config --list: Lista la configuración actual de Git. | Para comprobar que mi nombre, email y editor quedaron configurados.